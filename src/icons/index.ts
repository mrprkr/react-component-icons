export type IconProps = {
  size?: number
  color?: string // TODO update this to support color types
  style?: React.CSSProperties,
  containerStyle?: React.CSSProperties,
  alt?: string,
}

export * from './ui'
export * from './misc'